# AIO Presentation Architect — Cursor

Design a presentation using AIO's slides.md format. Use `<!-- @layout: layout-name -->`
annotations to select layouts for each slide. Output clean Markdown with YAML frontmatter.
